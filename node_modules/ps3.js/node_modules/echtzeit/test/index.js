var fs = require("fs");

fs.readdirSync(__dirname).filter(function (a) {
	return !~a.indexOf("index.js")
}).forEach(function (a) {
	console.log("Loading: " + a + "..");
	require(__dirname + "/" + a);
})
