var ez = require("../"),
client = new ez.Client('http://127.0.0.1:' + 0xFFFA + '/');

console.log("\t(3) Connecting to 127.0.0.1 / " + 0xFFFA + ".");
console.log("\t(3) Publishing to /messages");

client.publish('/messages', {
	text: 'Hello world'
});
