var http = require('http'),
    ez = require('../');

var server = http.createServer(),
    bayeux = new ez.NodeAdapter({mount: '/'});

bayeux.attach(server);
server.listen(0xFFFA);

console.log("\tListening on port", 0xFFFA);
